<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="info.android.rowing">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_LOCATION_EXTRA_COMMANDS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/rowing_icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="info.android.rowing.activity.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="info.android.rowing.activity.AboutUsActivity"
            android:label="@string/activity_title_about_us" />
        <activity
            android:name="info.android.rowing.activity.PrivacyPolicyActivity"
            android:label="@string/activity_title_privacy_policy" />
        <activity
            android:name=".activity.PlayerModeActivity"
            android:label="@string/new_session"/>
        <activity
            android:name=".activity.CoachModeActivity"
            android:label="@string/coach_mode" />
        <activity
            android:name=".activity.StartCoachModeActivity"
            android:label="@string/coach_mode"/>
        <activity
            android:name=".activity.StartPlayerModeActivity"
            android:label="@string/player_mode" />

    </application>

</manifest>